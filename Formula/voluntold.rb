# typed: false
# frozen_string_literal: true

# voluntold — the agent runtime CLI.
#
# AUTO-GENERATED by the voluntold release CI (cli_release →
# cli/scripts/render-formula.sh) and force-pushed on every release. Do not edit
# by hand; changes will be overwritten. The binaries are obfuscated with garble
# and published to https://dl.voluntold.ai.
class Voluntold < Formula
  desc "Agent runtime that routes issue-board work to AI coding agents"
  homepage "https://voluntold.ai"
  version "0.11.65"

  on_macos do
    on_arm do
      url "https://dl.voluntold.ai/v0.11.65/voluntold-darwin-arm64"
      sha256 "a200785f2a9b1b51fe457fc2c74aaaa77c34135e9ffdf634c12097dc74e9205f"
    end
    on_intel do
      url "https://dl.voluntold.ai/v0.11.65/voluntold-darwin-amd64"
      sha256 "0292dc08867f219471c613270e7ec7a8935067477e76cd0e07cda4f5a1a85e81"
    end
  end

  on_linux do
    on_arm do
      url "https://dl.voluntold.ai/v0.11.65/voluntold-linux-arm64"
      sha256 "bd9968641bf2caf0db48e9187bf6ae7bc4511d7ca6cdb93a6525f3b3a5013f6e"
    end
    on_intel do
      url "https://dl.voluntold.ai/v0.11.65/voluntold-linux-amd64"
      sha256 "9454c78e400e17506dbab8a664ca87f94cdcb0451ecf64afd4e2421010fbd8d8"
    end
  end

  def install
    # The download is a single bare binary named for the URL basename
    # (voluntold-<os>-<arch>). Plain HTTP downloads don't carry the executable
    # bit, so set it before installing as `voluntold` — otherwise generating
    # the completions (which execs the binary) fails with EACCES. Then emit the
    # bash/zsh/fish completions from the binary itself.
    binary = Dir["voluntold-*"].first
    chmod "+x", binary
    bin.install binary => "voluntold"
    generate_completions_from_executable(bin/"voluntold", "completion")
  end

  test do
    assert_match version.to_s, shell_output("#{bin}/voluntold --version")
  end
end
